export const todoReducer = (initialState = [],action) => {
    switch(action.tipe){
        case 'ABC':
            return initialState;
        default:
            return initialState;
    }
}